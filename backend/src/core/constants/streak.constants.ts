/** Bonus coins per streak day (streak * this value) */
export const STREAK_BONUS_COINS = 5;

/** Maximum bonus coins from streak per match */
export const MAX_STREAK_BONUS = 50;

/** Leaderboard: maximum entries returned */
export const LEADERBOARD_LIMIT = 50;
